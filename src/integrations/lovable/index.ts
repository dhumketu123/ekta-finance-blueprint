// Lovable platform integration — DEPRECATED post-migration to Blink.
// OAuth is now handled directly via supabase.auth.signInWithOAuth().
// This file is retained as a tombstone to prevent accidental re-import.
export const lovable = null;
